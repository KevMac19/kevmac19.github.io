version: '3'

services:
  web:
    container_name: kevmac-portfolio
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - 3000:3000
    environment:
      - NODE_ENV=production
